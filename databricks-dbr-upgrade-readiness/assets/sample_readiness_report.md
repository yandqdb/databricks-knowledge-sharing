# DBR Upgrade Readiness Report — `daily_sales_etl`

> Template. Replace bracketed values with the real workload. Generated by the
> `databricks-dbr-upgrade-readiness` skill.

## The jump

| | Source | Target |
|---|--------|--------|
| DBR | 10.4 LTS | 15.4 LTS |
| Apache Spark | 3.2 | 3.5 |
| Python | 3.8 | 3.11 |
| Scala | 2.12 | 2.12 (no recompile) |
| pandas | 1.x | 2.x |

This jump crosses **3 Spark minors** (3.2 → 3.3 → 3.4 → 3.5) → **staged path recommended** (see Upgrade path below).

## Verdict: **Ready with changes**

- 0 hard blockers (Category 3)
- 6 code-change sites (Category 2)
- 3 bridgeable findings (Category 1)
- Estimated effort: ~1 day of edits + 2 staged A/B test cycles.

## Findings

### Category 2 — code change needed

| Pattern | File:line | Severity | Fix |
|---------|-----------|----------|-----|
| `ansi-cast` | `etl/transform.py:88` | Blocker | `cast(amount AS INT)` on dirty strings will raise. Route to `databricks-ansi-remediation` → `try_cast`. |
| `pandas-append` | `etl/enrich.py:142` | Blocker | `df.append(row)` → `pd.concat([df, row_df])` (pandas 2.x). |
| `numpy-aliases` | `etl/enrich.py:30` | Blocker | `np.float` → `float` / `np.float64`. |
| `init-script-dbfs` | job JSON `init_scripts` | Blocker | `dbfs:/scripts/install.sh` → relocate to UC volume, update spec. |
| `sqlcontext` | `etl/legacy_load.py:12` | Warning | `sqlContext.sql(...)` → `spark.sql(...)`. |
| `delta-protocol-upgrade` | `etl/transform.py:201` | Blocker (coordination) | Writes `prod.sales.orders_agg`; confirm the BI job reading it is upgraded first or disable new table features. |

### Category 1 — bridgeable by config (temporary)

| Pattern | File:line | Bridge flag (temporary) | Follow-up |
|---------|-----------|-------------------------|-----------|
| `ansi-cast` (the rest) | multiple | `spark.sql.ansi.enabled=false` | Remove after try_cast rewrite lands + parity holds. |
| `time-parser-legacy` | `etl/transform.py:54` | `spark.sql.legacy.timeParserPolicy=LEGACY` | Fix `to_timestamp('YYYY...')` pattern, then remove flag. |
| `negative-scale-decimal` | `etl/transform.py:77` | `spark.sql.legacy.allowNegativeScaleOfDecimal=true` | Widen decimal type, then remove flag. |

### Category 3 — hard blockers

None.

## Upgrade path

```
10.4 (3.2)  →  12.2 LTS (3.3)  →  14.3 LTS (3.5)  →  15.4 LTS (3.5, Py 3.11)
```

- **Stage 1 (3.2→3.3):** ANSI + datetime + decimal findings surface here. Set bridge flags, land, A/B test.
- **Stage 2 (3.3→3.5):** interval-literal + removed-functions; re-test.
- **Stage 3 (→15.4):** Python 3.8→3.11 + pandas 2.x + numpy findings; fix code, re-test.

## Bridge flags to set (then remove)

```python
spark.conf.set("spark.sql.ansi.enabled", "false")
spark.conf.set("spark.sql.legacy.timeParserPolicy", "LEGACY")
spark.conf.set("spark.sql.legacy.allowNegativeScaleOfDecimal", "true")
```

## A/B parity result

| Stage | Row count | Schema | Row diff | Status |
|-------|-----------|--------|----------|--------|
| 1 (12.2) | match | match | 0 | ✓ |
| 2 (14.3) | match | match | 0 | ✓ |
| 3 (15.4) | match | match | 0 | ✓ |

## Delta protocol impact

- Writes: `prod.sales.orders_agg`.
- Readers: `bi_dashboard_refresh` (currently DBR 11.3). **Upgrade that job first**, or set `delta.enableDeletionVectors=false` on the table until it is upgraded.

## Cutover & rollback

- Cutover: bump `spark_version` to `15.4.x-scala2.12` on the production job; monitor first 3 scheduled runs.
- Rollback: revert `spark_version` to source; re-pin libraries. Note: a Delta protocol upgrade on `orders_agg` is **not** trivially reversible — that is why the A/B ran against test tables.
